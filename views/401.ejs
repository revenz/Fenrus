<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/x-icon" href="/favicon.svg?version=<%= version %>">
    <title>Fenrus - Unauthorized</title>
    <link rel="stylesheet" href="/css/site.min.css?version=<%= version %>" />
    <link id="theme-style" rel="stylesheet" href="/themes/Default/_theme.css?version=<%= version %>" />
    <link rel="stylesheet" href="/css/login.css?version=<%= version %>" />

    <% if(redirectUrl) { %>
        <meta http-equiv="refresh" content="10; url=<%= redirectUrl %>">
    <% } %>
    
    <style>
        .message-box {
            background:rgba(255,0,0, 0.7);
            width:min(50rem, 70vw);
            margin:auto;
            padding:2rem;
            border-radius: 0.5rem;
            text-align: center;
        }
        .link
        {
            margin-top:1rem;         
        }
        .link  a {
            color:white;   
        }
    </style>
</head>

<body class="401 no-custom-background animate-background">
    <div class="bg bg1"></div>
    <div class="bg bg2"></div>
    <div class="bg bg3"></div>

    <div class="main-content">

        <div class="message-box">
            <div class="message">
                <%= message || 'You attempted to perform an authorized operation.' %>
            </div>
    
            <% if(redirectUrl) { %>
                <div class="link">
                    <a href="<%= redirectUrl %>">Back</a>
                </div>
            <% } else if(loginUrl) { %>
                <div class="link">
                    <a href="<%= loginUrl %>">Login</a>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
