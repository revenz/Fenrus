@namespace Fenrus.Components.Inputs
@inherits Input<string>

<InputWrapper Input="@this">
    <input type="@(Password ? "password" : Email ? "email" : "text")" @bind-value="@Value" autocomplete="none" />
</InputWrapper>